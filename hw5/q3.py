import matplotlib.pyplot as plt
import numpy as np


def PolyEval(n, a, y, x):
    # YOUR CODE HERE
    return ...

n = ... # take it from the task
a = [...] # take it from the task
y = [...]  # take it from the task
x_eval = ... # take it from the task

value = PolyEval(n, a, y, x_eval)